fix: all unclickable buttons + Clerk redirect to /app after auth

- Replace all Button render={<Link>} with plain <Link> elements
  across navbar, hero, CTA section, and pricing cards (Base UI
  Button was not forwarding click events)
- Add forceRedirectUrl="/app" to ClerkProvider, SignIn, and SignUp
  so users land on the dashboard after login/registration
- Make Hero "Générer" button navigate to /register with prompt
