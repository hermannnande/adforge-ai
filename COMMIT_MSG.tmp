fix: remove interactive transaction + add dashboard fallback UI

- Replace $transaction callback with sequential queries (Neon
  serverless adapter does not support interactive transactions)
- Add try/catch with a friendly fallback page when workspace
  provisioning is still in progress
- Add duplicate-check before creating user to prevent constraint
  violations
