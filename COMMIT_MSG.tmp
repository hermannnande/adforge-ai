fix: replace Button render={<Link>} with plain <Link> in navbar

The Base UI Button with render prop was not forwarding click events
correctly, making nav links unclickable. Use plain Next.js Link
elements with matching styles instead.
